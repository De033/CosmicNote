#import "../index.typ": *

= 快速开始

欢迎使用 CosmicNote。这是一个专为长篇课程笔记、学术排版设计的高级 Typst 模板。本版本在传统模板基础上，引入了 Tufte 风格的超大侧边栏与现代横屏美学，力求为您带来极致的排版与阅读体验。

== 核心理念

我们摒弃了高饱和度的原色，转而使用更加沉稳的 *幽灵灰 (Geistergrau)* 作为护眼背景色，以及 *冷钢蓝 (Steel Blue)* 作为主体的视觉引导色。这种色彩搭配在长时间阅读和学习中，能极大缓解视觉疲劳，并保持一种严肃的学术质感。

== 横屏排版的魅力

传统的 A4 纸张在分配给主文本阅读区后，留给边栏的空间往往捉襟见肘。而 CosmicNote 默认采用了 A4 横屏（Landscape）的尺寸设计：
- *左侧* 拥有约 15cm 的舒适阅读区
- *右侧* 拓展出了高达 12.2cm 的专属边栏（Margin）

这使得您能够优雅地将图片、代码解释、参考资料或公式注解放在正文的旁边，无需让读者反复前后翻页寻找对应内容。

#tufted.margin-note[
  *这是什么？*
  
  这就是 Tufte 排版的灵魂：旁注（Margin Note）。
  在 CosmicNote 中，只需调用 `#tufted.margin-note[]`，内容就会自动被推算到这个宽阔的边栏中，并且绝对对齐于您在左侧正文中引用的位置。
]

== 开始写作

要开始编写您自己的笔记，您只需要：
+ 修改 `main.typ` 中的标题、作者和封面信息。
+ 在 `content` 目录下创建新的 `.typ` 文件。
+ 如果要在子文件中使用旁注或超宽区域等高级特性，请在文件顶部加上 `#import "../index.typ": *`。
+ 在 `main.typ` 中使用 `#include` 将您的新章节引入。

就是这么简单。现在，请翻到下一页，看看我们为您预设好的各类排版组件。
